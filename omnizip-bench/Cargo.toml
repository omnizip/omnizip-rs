[package]
name = "omnizip-bench"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Benchmark suite for omnizip-rs codecs (Silesia, Enwik8, Calgary, Canterbury corpora)"
keywords = ["compression", "benchmark", "silesia", "enwik8"]
categories = ["compression", "development-tools::profiling"]

[[bin]]
name = "omnizip-bench"
path = "src/main.rs"

[lib]
name = "omnizip_bench"
path = "src/lib.rs"

[dependencies]
omnizip-codecs = { path = "../omnizip-codecs", version = "0.14.20" }
omnizip-lzma = { path = "../omnizip-lzma", version = "0.14.20" }
omnizip-zstd = { path = "../omnizip-zstd", version = "0.14.20" }
omnizip-snappy = { path = "../omnizip-snappy", version = "0.14.20" }
omnizip-lz4 = { path = "../omnizip-lz4", version = "0.14.20" }
omnizip-deflate = { path = "../omnizip-deflate", version = "0.14.20" }
omnizip-deflate64 = { path = "../omnizip-deflate64", version = "0.14.20" }
omnizip-brotli = { path = "../omnizip-brotli", version = "0.14.20" }
omnizip-bzip2 = { path = "../omnizip-bzip2", version = "0.14.20" }
omnizip-ppmd = { path = "../omnizip-ppmd", version = "0.14.20" }
omnizip-zpaq = { path = "../omnizip-zpaq", version = "0.14.20" }
omnizip-glza = { path = "../omnizip-glza", version = "0.14.20" }
omnizip-flac = { path = "../omnizip-flac", version = "0.14.20" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.5", features = ["derive"] }
ureq = { version = "2.12", default-features = false, features = ["tls"] }
zip = { version = "2.2", default-features = false, features = ["deflate"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
pedantic = "warn"

[features]
# Forward to omnizip-flac's SIMD path for quick_perf comparison.
simd-lpc = ["omnizip-flac/simd-lpc"]
