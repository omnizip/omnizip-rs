[package]
name = "omnizip-libdeflate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Pure-Rust libdeflate-compatible DEFLATE codec — in-house RFC 1951 encoder + decoder, no external deps"
keywords = ["compression", "deflate", "libdeflate", "fast"]
categories = ["compression"]

[lib]
name = "omnizip_libdeflate"
path = "src/lib.rs"

[dependencies]
omnizip-codecs.workspace = true

[lints]
workspace = true
