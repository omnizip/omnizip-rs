[package]
publish = false
name = "omnizip-archive-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared archive-container layer: entry model, reader/writer traits, security boundary, format detection, gzip/bzip2 single-file formats"

[dependencies]
omnizip-libdeflate = { path = "../omnizip-libdeflate", version = "0.21.40" }
omnizip-lzma = { path = "../omnizip-lzma", version = "0.21.40" }
omnizip-bzip2 = { path = "../omnizip-bzip2", version = "0.21.40" }

[lints]
workspace = true
