[package]
name = "omnizip-tar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "TAR container (ustar + GNU long names + pax read) on omnizip-archive-core"

[dependencies]
omnizip-archive-core = { path = "../omnizip-archive-core", version = "0.21.105" }

[lints]
workspace = true
