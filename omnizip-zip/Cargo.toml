[package]
name = "omnizip-zip"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "ZIP container (store/deflate/bzip2/zstd methods, ZIP64) on omnizip-archive-core"

[dependencies]
omnizip-archive-core = { path = "../omnizip-archive-core" }
omnizip-libdeflate = { path = "../omnizip-libdeflate" }
omnizip-bzip2 = { path = "../omnizip-bzip2" }
omnizip-zstd = { path = "../omnizip-zstd" }
omnizip-crypto = { path = "../omnizip-crypto" }

[lints]
workspace = true
