[package]
name = "omnizip-deflate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Pure-Rust DEFLATE codec — delegates to omnizip-libdeflate (no external deps)"
keywords = ["compression", "deflate", "zlib", "gzip"]
categories = ["compression"]

[lib]
name = "omnizip_deflate"
path = "src/lib.rs"

[dependencies]
omnizip-codecs.workspace = true
omnizip-libdeflate = { path = "../omnizip-libdeflate", version = "0.19.0" }

[lints]
workspace = true
