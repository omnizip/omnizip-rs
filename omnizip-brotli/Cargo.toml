[package]
name = "omnizip-brotli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Pure-Rust Brotli codec — from-spec RFC 7932 implementation behind the omnizip Codec trait"
keywords = ["compression", "brotli", "codec"]
categories = ["compression"]

[lib]
name = "omnizip_brotli"
path = "src/lib.rs"

[dependencies]
omnizip-codecs.workspace = true

[dev-dependencies]
brotli.workspace = true

[lints]
workspace = true
