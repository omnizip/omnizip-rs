[package]
name = "omnizip-rar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "RAR archives — RAR5 read/write (store) + RAR4 read (store), VINT headers, CRC32 verification on omnizip-archive-core"
keywords = ["compression", "rar", "rar5"]
categories = ["compression"]

[lib]
name = "omnizip_rar"
path = "src/lib.rs"

[dependencies]
omnizip-archive-core = { path = "../omnizip-archive-core", version = "0.21.112" }
omnizip-crypto = { path = "../omnizip-crypto", version = "0.21.112" }

[lints]
workspace = true
